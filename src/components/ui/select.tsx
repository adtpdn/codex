import { Select as AntSelect } from 'antd';
import type { SelectProps } from 'antd';

export const Select = AntSelect;
export const { Option: SelectOption } = AntSelect;
export const { OptGroup: SelectOptGroup } = AntSelect;